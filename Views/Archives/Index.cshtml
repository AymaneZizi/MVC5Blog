@model List<MVC5Blog.Models.MvcMovie.Models.Post>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@foreach(var item in Model)
{
    <h3>@Html.ActionLink(@item.Title, "Post_Detail", new { post_Id = item.ID }) </h3>
}